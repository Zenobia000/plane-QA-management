# Testing 產品定義 —— Persona、Jobs-to-be-Done、介面設計與需求分類

> 狀態:草稿 v0.7 · 2026-07-28 · #11 已完成;新增 #14–#19
> 範圍:自用、多團隊多專案 · 執行形式:混合(關鍵路徑自動化)
> 這份文件是**共同基礎、已排序的 backlog 與介面設計判準**,不是實作規格。每個缺口各自走一輪 spec → plan → 實作。

## 論點

這套系統的價值全部押在一條鏈上:一個需求如何變成可執行的驗收契約、如何被驗證、失敗如何變成可重現的任務、修好後如何回到證據裡。

鏈是完整的 —— **資料層完整,呈現層斷裂**。以下每個斷點都已在程式碼中定位。

## 證據鏈與它的斷裂處

```
需求 ──✕── 驗收契約 ──── 一輪驗證 ──── 證據 ──✕── 缺陷 ──✕── 重現與修復 ──✕── 重驗
Work item      Test case      Test run      Result    Defect issue    RD / RD agent   Retest result
```

`✕` 表示資料層存在關聯,但使用者在介面上走不過去。

四道斷裂與其成因:

| 斷裂        | 成因                                                                          |
| ----------- | ----------------------------------------------------------------------------- |
| 需求 ↔ 契約 | 連結要手貼 UUID;建立後只顯示數量、不可點、無法解除                            |
| 證據 → 缺陷 | `create_defect_from_result` 只撈 `actual_result.get("text")`,附件與截圖被剝除 |
| 缺陷 → 重現 | 缺陷描述回不到來源 run / case                                                 |
| 重現 → 重驗 | 要自己翻回原本那一輪                                                          |

共同成因曾是:Testing 分頁沒有 URL 路由,任何深連結都做不出來。**該阻擋項已於 #11 解除** —— 每個實體現在都可定址(見附錄 C 之後的路由結構)。四道斷裂仍在,但已從「技術上做不到」變成「尚未接上」。

---

## 第 1 節 · Persona

四個人類 persona 加兩個系統 actor。刻意壓到最少 —— 角色會依團隊成熟度崩塌(有專職 QA 的團隊與沒有的團隊共用這份文件),所以「同一個人做多件事」交給 JTBD 主幹處理,persona 本身要穩定。

### P1 · 測試設計者

- **誰扮演**:有專職 QA 的團隊 = QA analyst;沒有的團隊 = PM 或資深 dev
- **核心目標**:讓每個需求都有明確、可執行、不會過期的驗收契約
- **成功長什麼樣**:coverage 沒有未覆蓋的需求;別人照著 case 就能跑
- **現在最痛**:連結需求要手貼 UUID;看不到哪些情境已被自動化覆蓋,因此重複設計

### P2 · 執行者

- **誰扮演**:QA;小團隊可能是 dev 互測
- **核心目標**:在一次 build 裡跑完該跑的,並留下可信的證據
- **成功長什麼樣**:一輪跑完不用切視窗;失敗能一鍵變成有證據的缺陷
- **現在最痛**:執行體驗其實已經不錯(`execution-workspace.tsx` 已有 P/F/B/S 鍵盤流)。真正的痛是**證據記不下來** —— 貼不了圖、傳不了檔;以及重驗時要自己翻回原本那一輪

### P3 · 開發者

- **誰扮演**:RD。身兼「自動化作者」與「缺陷修復者」兩個 job,由主幹區分
- **核心工作流**:接任務 → **照 QA 的步驟重現同樣現象** → 修復 → 回報狀態
- **成敗關鍵**:是「重現」不是「修」。重現需要完整步驟、前置條件、**截圖或錄影**、實際觀察
- **現在最痛**:
  1. 缺陷描述只有純文字,截圖與附件在建立當下就被剝除
  2. 缺陷回不去來源 run / case,看不到該 case 的其他執行歷史
  3. (另一頂帽子)CI 自動建立的 case 會孤兒化,介面沒有任何待歸檔提示

### P4 · 交付決策者

- **誰扮演**:PM / PO
- **核心目標**:回答「這個需求驗過了嗎」與「這一版能不能出」
- **成功長什麼樣**:不進 Testing 分頁也能知道狀態;出貨決策有依據,不是憑印象
- **現在最痛**:需求頁上的覆蓋率面板是死的(`issue-detail/testing-summary.tsx`)—— 看得到 TC-3 失敗,點不進去

### S1 · CI Pipeline(系統 actor)

- **能力**:冪等寫入 —— 同 key 重放回 200,payload 變了回 409 而非靜默覆蓋
- **設計時不能忘**:它會**自動產生 test case**。未映射的結果會建立標記 `automated` 的 case
- **現況**:上傳可挾帶 artifact —— **人工執行反而不行**,這是個不對稱

### S2 · 開發者的 Agent(系統 actor)

- **能力**:透過 MCP / CLI 抓取任務細節 → 依證據重現 → 修復 → 回寫狀態
- **為何列為一級**:這是本系統相對 Jira / TestRail 的**差異化能力**。只考慮人類介面等於放棄它
- **設計約束**:**人在介面上能做的,agent 都要能做到同樣完整;agent 寫入的內容,人也要能在介面上看懂並追溯**
- **現在最痛**:agent 拿到的證據跟人一樣殘缺 —— 描述被剝成純文字後,再聰明的 agent 也重現不了

---

## 第 2 節 · JTBD 主幹

六件要完成的工作。斷點標在它切斷的**那一步**上。

### J1 · 定義驗收契約

> P1 主導 · P4 驗收 DoR

| #   | 步驟                      | 狀態                                       |
| --- | ------------------------- | ------------------------------------------ |
| 1   | 讀需求                    | —                                          |
| 2   | 設計情境(happy + unhappy) | **#7** 看不到已被自動化覆蓋的情境,重複設計 |
| 3   | 寫 steps 與前置條件       | —                                          |
| 4   | 歸檔到資料夾              | ✅ **#8** 刪除與改名已補上                 |
| 5   | 連結需求                  | **#1** 手貼 UUID,應可搜尋選取              |
| 6   | 確認覆蓋率                | **#4** 只顯示數量,不可點、無法解除連結     |

### J2 · 排一輪驗證

> P2 主導 · P4 定範圍

| #   | 步驟                     | 狀態                                                 |
| --- | ------------------------ | ---------------------------------------------------- |
| 1   | 決定範圍(sprint / build) | **#10** 介面無法把 run 綁到 cycle,sprint 與 run 斷開 |
| 2   | 挑選 case                | **#9** 扁平清單,無分組、搜尋、篩選、批次選取         |
| 3   | 跨分頁往返               | **#12** 要先在 Test cases 挑,再到 Test runs 建       |
| 4   | 建立 run(釘住 case 版本) | —                                                    |

### J3 · 執行並留下證據

> P2 主導 · S1 自動路徑

| #   | 步驟                        | 狀態                                              |
| --- | --------------------------- | ------------------------------------------------- |
| 1   | 逐 case 執行                | ✅ P/F/B/S 鍵盤流已完整                           |
| 2   | 記錄狀態                    | —                                                 |
| 3   | 寫實際結果                  | —                                                 |
| 4   | 附上證據(截圖 / log / 連結) | **#2** 完全做不到。CI 卻可挾帶 artifact —— 不對稱 |

### J4 · 追一個失敗到修好

> P2 → P3 → S2 agent 路徑 → P2

| #   | 步驟                      | 狀態                                     |
| --- | ------------------------- | ---------------------------------------- |
| 1   | 判定失敗                  | —                                        |
| 2   | 建立缺陷                  | **#3** 只帶純文字,截圖與附件被剝除       |
| 3   | 指派 RD 或其 agent        | —                                        |
| 4   | **重現現象**              | **#3 的後果** 證據不足以重現,得回頭問 QA |
| 5   | 修復                      | —                                        |
| 6   | 回報狀態(可由 agent 寫回) | —                                        |
| 7   | QA 重驗                   | ✅ **#11** 已可定址,缺的是缺陷回連(#3)   |
| 8   | 關閉(結果永遠追加不覆寫)  | —                                        |

### J5 · 讓自動化結果有意義

> S1 · P3

| #   | 步驟                           | 狀態                                        |
| --- | ------------------------------ | ------------------------------------------- |
| 1   | CI 執行                        | —                                           |
| 2   | 冪等上傳                       | ✅ idempotency key 語意完整                 |
| 3   | 映射到既有 case(`external_id`) | —                                           |
| 4   | 未映射者自動建 case            | —                                           |
| 5   | 人回來歸檔並連需求             | **#7** 沒有任何待歸檔提示,於是永久孤兒      |
| 6   | 手動與自動並存呈現             | **#13** 同一需求下兩條路徑如何合看,尚未設計 |

### J6 · 決定能不能出貨

> P4 主導 · P2 提供證據

| #   | 步驟                 | 狀態                                |
| --- | -------------------- | ----------------------------------- |
| 1   | 查看覆蓋率           | **#5** 需求頁面板不可點,跳不回 case |
| 2   | 檢視未結缺陷         | **#6** case 沒有執行歷史可回溯      |
| 3   | 出貨閘門             | ✅ 機讀判定完整可信                 |
| 4   | 做出決策             | —                                   |
| 5   | 未完成項捲動至下一輪 | —                                   |

---

## 第 3 節 · 缺口 backlog

跨 persona 的系統性問題,不歸屬單一角色。

~~**#11 是基礎阻擋項**~~ —— **已完成**。#3、#5、#6 的導覽修復現在技術上可行。
**#2 是 #3 的上游** —— 先有附件能力,才談得上把證據帶進缺陷。
**#16 是 #14 的前置** —— 術語定案後,階層 roll-up 才知道該用什麼詞呈現。詳見第 5 節。
**#17 是 #15 的上游** —— 先能記錄量測值,出貨門檻才有東西可引用。
**#14 與 #18 是同一條線** —— 覆蓋率算對了(#14),門檻才有正確的數字可用(#18)。兩者皆由 DEMO 範例專案實證,見下方。

| #   | 缺口                                                                                                                                                                                       | 影響     | 嚴重度    | 相依        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --------- | ----------- |
| 14  | coverage 報表把每個 work item 都當需求:不沿階層 roll-up、不濾型別(缺陷也列入)、且 `coverage_percent` 算的是「已連結需求的 case 佔比」而非「已被覆蓋的需求佔比」(`report.py:60-63, 92-123`) | P1 P4    | **阻擋**  | 需 #16      |
| 18  | release gate 完全不看覆蓋率:需求零驗收契約仍判定可出貨,DoR 從未在門檻上被強制(`report.py:83`)                                                                                              | P4 P1    | **阻擋**  | 需 #14      |
| 17  | test case 缺 `type` 與結構化門檻,無法區分驗證方式、承載量測值與趨勢                                                                                                                        | P2 P3 P4 | 高        | #15 的上游  |
| 15  | release gate 無外部證據接口:SLO、掃描、審查簽核進不了出貨判定(`report.py:83`)                                                                                                              | P4 P2    | 高        | 需 #17      |
| 16  | testing UI 未接 i18n,術語無法軟體領域化(其他功能皆已接)                                                                                                                                    | 全體     | 高        | 擋住 #14    |
| 19  | 資料夾只渲染成扁平清單,`TestFolder.parent` 的階層在介面上看不見(WBS 2.8 誤標為已交付 folder tree)                                                                                          | P1       | 中        | —           |
| 2   | 實際結果無法上傳檔案、貼圖、嵌入連結(Testing 路由樹無 asset 端點)                                                                                                                          | P2 P3    | 高        | #3 的上游   |
| 3   | 缺陷描述只帶純文字,證據被剝除                                                                                                                                                              | P3 S2    | 高        | 需 #2       |
| 5   | 需求頁的覆蓋率面板不可點,跳不回 case                                                                                                                                                       | P4       | 高        | #11 已解除  |
| 6   | case 詳情沒有執行歷史(`latest_status` 有值卻未呈現)                                                                                                                                        | P1 P3    | 高        | #11 已解除  |
| 10  | 介面無法把 run 綁到 cycle / module(型別支援 `cycle_id`,builder 未送出)                                                                                                                     | P2 P4    | 高        | —           |
| 1   | 連結需求要手貼 UUID,應改為可搜尋選取                                                                                                                                                       | P1       | 中        | —           |
| 4   | 需求連結只顯示數量,不可點、無法解除(服務層缺 unlink)                                                                                                                                       | P1 P4    | 中        | —           |
| 7   | 自動建立的孤兒 case 沒有待歸檔提示                                                                                                                                                         | P3 P1    | 中        | —           |
| 9   | run builder 是扁平清單,無分組 / 搜尋 / 篩選 / 批次選取                                                                                                                                     | P2       | 中        | —           |
| 12  | 建立 run 需跨分頁往返,流程不連貫                                                                                                                                                           | P2       | 中        | 關聯 #9 #10 |
| 13  | 同一需求下手動與自動 case 如何合併呈現,尚未設計                                                                                                                                            | P1 P3    | 中        | 關聯 #7     |
| 11  | Testing 分頁沒有 URL 路由,無法深連結到特定分頁 / case / run                                                                                                                                | P3 P4 S2 | ✅ 已完成 | 解鎖 #5 #6  |
| 8   | 測試資料夾無法刪除或改名                                                                                                                                                                   | P1       | ✅ 已完成 | —           |

---

## 附錄 C · 四個實體的生命週期

這四個實體的不變量是系統的核心保證 —— 它們已經在模型層被強制,設計時只能順著走,不能繞過。

### 驗收契約(Test case)

```
建立 → 編輯:發布新的不可變版本 → 連結需求 → 被 run 釘住版本 → [封存]
```

- **不變量**:編輯永遠發布新版本,舊版本永遠可讀。歷史不被改寫
- **誰碰得到**:P1 建立維護 · P3 自動化 case · P2 執行時讀
- **缺什麼**:執行歷史沒有回顯(#6);需求連結是啞的(#1 #4);版本歷史無法瀏覽

### 一輪驗證(Test run)

```
建立:釘住當下的 case 版本 → 執行中 → [關閉:拒絕新結果]
```

- **不變量**:建立時釘住版本,契約之後的編輯不影響已存在的 run。關閉後拒絕一切結果
- **誰碰得到**:P2 建立與執行 · S1 自動建立 · P4 讀進度
- **缺什麼**:綁不到 sprint(#10);挑 case 的體驗破碎(#9 #12)

### 證據(Test result)

```
追加 → [永不修改、永不刪除]
```

- **不變量**:只能追加。更正錯誤的方式是**追加一筆新結果並在內容說明**,不是改舊的
- **誰碰得到**:P2 記錄 · S1 上傳 · P3 與 S2 讀取以重現
- **缺什麼**:無法附上任何證據(#2)—— 這是整條鏈上游的源頭

### 缺陷(Defect = 真正的 Plane work item)

```
從 failed / blocked 原子建立 → 進入 backlog → 修復 → [狀態群組進 completed / cancelled 才算關閉]
```

- **不變量**:只能從失敗或受阻的結果建立,且與結果連結是同一筆交易。它是真的 work item,走一般 issue 流程
- **誰碰得到**:P2 建立 · P3 與 S2 修復 · P4 排程與出貨判定
- **缺什麼**:證據在建立當下被剝除(#3);回不去來源 run / case —— 路由已備妥(#11),缺的是把連結接上

---

## 可追溯性設計原則

這些原則用來裁決後續每一個介面決定。它們不是功能清單,是**判準**:任何新設計違反其中一條,就退回重想。

### 每一個關聯都要雙向可走

現況的關聯幾乎都是單向可見:A 看得到 B 存在,卻點不過去;或 B 根本不知道 A 的存在。凡是資料層有連結的地方,**兩端都要有可點的入口**。

- **契約 ↔ 需求**:契約要列出需求的名稱與狀態且可點;需求頁的覆蓋率面板每列可點回契約
- **證據 → 缺陷**:已有。**缺陷 → 證據 / 一輪驗證 / 契約**:缺
- **契約 → 歷次驗證**:缺。這是 P1 與 P3 都要的視角

### 證據必須完整跨越交接點

QA 記下的東西,RD 或其 agent 要**原封不動**拿到。任何在交接時剝除資訊的轉換都是缺陷,不是取捨。這條原則直接來自 J4 的失敗模式。

- 實際結果的富文本與附件,要完整帶進缺陷描述
- 附件實體要能同時被結果與缺陷 work item 參照,不是複製兩份

### 人與 agent 同能力

凡人在介面上能做的,MCP 與 CLI 都要能做到同樣完整;凡 agent 寫入的內容,人在介面上都要看得懂並能追溯。這是 S2 存在的前提,也是這套系統相對商業工具的差異點。

- 新增任何介面動作時,同步確認對應的 MCP tool 與 CLI 指令存在且參數等價
- agent 的寫入要留下可辨識的來源,人才能判斷這筆是誰做的

### 深連結是基礎設施,不是功能

每個實體都要有可定址的網址。這是前面三條原則的**技術前提** —— 沒有它,「可點過去」在實作上無從談起。

**此原則已落實(#11)**,以下路由結構已上線;新增任何實體時比照辦理。

已上線的路由結構:

```
/testing                         → 導向 overview
/testing/overview
/testing/cases                   → 可帶 ?folder= 篩選
/testing/cases/:sequence         → 單一契約,需求頁由此連入
/testing/runs
/testing/runs/:runId             → 一輪驗證
/testing/runs/:runId/:runCaseId  → 單一執行項,缺陷由此連回
```

### 不可變性要在介面上被看見

版本化、追加式結果、釘住版本 —— 這些是系統最有價值的保證,但使用者現在幾乎感覺不到。部分已呈現(編輯時會提示將產生新版本、執行時顯示釘住的版本號),但**看不到歷史本身**。

- 契約要能瀏覽版本歷史,並看出哪一版被哪些驗證釘住
- 結果以時間軸呈現每次追加,讓「失敗 → 修復 → 重驗」的歷程可讀

---

## 第 4 節 · 介面設計

三個介面,對應三個 persona 的關鍵時刻:P1 定義契約、P2 與 P3 交接證據、P4 判斷可否出貨。設計以前述五條原則為判準,並遵守一條額外的總則。

### 總則:優先複用 Plane 自己的互動元件

Testing 之所以感覺像外掛,正是因為它**重新發明了原生已有的原語** —— 需求連結做成一個貼 UUID 的輸入框,而隔壁 work item 的關聯早就有搜尋 modal。以下設計幾乎不需要新元件,只需要把既有的接上來。這同時是最省工與最能讓 testing 讀起來像原生功能的做法。

| 需要的能力                   | Plane 既有元件                                      | 解掉     |
| ---------------------------- | --------------------------------------------------- | -------- |
| 可搜尋的需求選擇器           | `ExistingIssuesListModal`                           | #1       |
| 連結顯示為可點 chip 且可移除 | `IssueRelationSelect` 模式 · `generateWorkItemLink` | #4       |
| 富文本編輯(含貼圖)           | `DescriptionInput`                                  | #2       |
| 附件上傳 / 清單 / 刪除       | `issues/attachment/*`                               | #2       |
| 版本歷史瀏覽                 | `DescriptionVersionsRoot` 模式                      | 契約版本 |
| 疊層詳情與深連結             | `issues/peek-overview/`                             | #11      |

### 介面一 · 契約詳情

> 路由:`/testing/cases/:sequence`

從清單點入開 peek 疊層,可直接以網址進入全頁。兩者共用同一個內容元件,差別只在容器。

版面:標題列(`TC-12` · `v3` · 手動/自動徽章 · 全頁開啟)→ 分頁(契約 / 歷次驗證 / 版本歷史)→ 內容。

| 區塊         | 設計                                                                                                                                                                       | 解掉  |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| 需求連結     | 「＋ 連結需求」開 `ExistingIssuesListModal` 搜尋選取,不再貼 UUID。已連結者顯示為 chip:識別碼、標題、狀態圓點,整塊可點到 work item,**✕ 可解除**。服務層目前缺 unlink,需補上 | #1 #4 |
| 空狀態       | 「尚未連結需求 —— **未連結的契約不會出現在覆蓋率報表**」。空狀態要說出後果,不只說沒有東西                                                                                  | —     |
| 歷次驗證分頁 | 每列一次執行:一輪驗證名稱(可點)、build、**當時釘住的版本**、結果、執行者、時間。產生過缺陷者顯示缺陷 chip 可點進 work item。這是契約 → 驗證 → 缺陷的回頭路                 | #6    |
| 版本歷史分頁 | 列出 v1…vN 並標示**哪一版被哪些驗證釘住** —— 讓不可變性看得見,而不只是寫在文件裡                                                                                           | —     |
| 來源徽章     | 標題列標示**手動**或**自動**。自動來源的契約在清單可篩選,配合待歸檔提示解 #7                                                                                               | #13   |

### 介面二 · 執行工作區

> 路由:`/testing/runs/:runId/:runCaseId`

三欄結構與 P/F/B/S 鍵盤流保留 —— 它們已經好用。只改「實際結果」與「缺陷」兩區。

| 區塊              | 設計                                                                                                                                                                | 解掉 |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 實際結果          | 純文字框換成 `DescriptionInput`:粗體、清單、連結、**直接貼上截圖**。下方附件區複用 `issues/attachment/*` 的拖放上傳與清單。Testing 路由樹目前沒有 asset 端點,需新增 | #2   |
| 建立缺陷前先預覽  | 明示**將帶過去的內容**含附件數量,再讓人按下建立。P3 的成敗關鍵是重現,而重現靠的就是這一包東西完整送達                                                               | #3   |
| 附件用參照,不複製 | 同一份附件同時被結果與缺陷 work item 參照。複製兩份會讓兩邊各自演化,日後看缺陷時無法確定看到的是不是當時那張圖                                                      | #3   |
| 建立後雙向可走    | 缺陷顯示為可點 chip;缺陷 work item 那端也要能**回到這次執行**                                                                                                       | #11  |

### 介面三 · 需求頁的驗證面板

> 位置:work item 詳情內

P4 的主要入口。他不進 Testing 分頁,所以這塊要能獨立回答「這個需求驗過了嗎」。

版面:頂部一行結論式摘要(`4 個契約 · 1 失敗 · 1 未執行 · 2 通過`)→ 契約清單,失敗者就地展開。

| 區塊               | 設計                                                                                                               | 解掉 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------ | ---- |
| 每列可點           | 整列連到 `/testing/cases/:sequence`。現況是純文字,看得到 TC-12 失敗卻點不進去                                      | #5   |
| 失敗要就地展開後果 | 失敗的契約下方直接顯示**是哪一輪驗證失敗的**與**它產生的缺陷**,兩者皆可點。P4 不需要為了知道「卡在哪」而離開需求頁 | #5   |
| 摘要先於明細       | 頂部一行結論式摘要。P4 多數時候只讀這一行                                                                          | —    |
| 來源徽章而非分組   | 手動與自動**混在同一份清單**,以徽章區分。對 P4 而言問題是「驗過沒」,來源是次要資訊;分成兩組會讓他讀兩次才得到答案  | #13  |
| 空狀態要能行動     | 「尚未連結任何驗收契約」並附上**建立契約**的入口 —— 這是 DoR 未達成的訊號,不是空白                                 | —    |

### 實作陷阱

**換掉編輯器會打壞既有的鍵盤流。** 執行工作區的 P/F/B/S 快捷鍵目前**只擋 `HTMLInputElement` 與 `HTMLTextAreaElement`**。富文本編輯器是 `contentEditable`,不屬於這兩者 —— 換上去之後,在編輯器裡打字的每一個 p、f、b、s 都會直接送出一筆結果。這是 append-only 的資料,**送出就收不回來**。改動時必須同步擴充焦點判斷,並補一個回歸測試。

~~**深連結必須先於這三個介面。**~~ **已完成(#11)** —— 介面一的全頁與 peek、介面二的執行項網址、介面三的每一列連結所需的路由都已存在。三個介面現在可以各自獨立進行。

---

## 第 5 節 · 企業敏捷層級與需求分類

本節回答一個先前沒處理的問題:這套系統要不要、以及能不能承載企業敏捷的需求結構。結論是**骨架可以,但有三處實質落差**,構成 #14 #15 #16。

### 兩個不同維度,不是同一條鏈

常見的誤解是把它們串成一條:`Epic → Feature → Story → FR → NFR → Task`。這是錯的。

- **Epic / Feature / Story / Task** 是**工作拆解結構**,回答「工作要怎麼拆、怎麼排進開發」
- **FR / NFR** 是**需求性質分類**,回答「系統要做什麼、要做到什麼程度」

兩者是交叉關係:FR 與 NFR 橫跨每一個工作層級。

```
Business Goal
└── Epic
    └── Feature
        ├── Story
        │   ├── FR / Acceptance Criteria
        │   ├── BDD Scenario
        │   └── Task
        └── Feature-level NFR
System
└── System-level NFR
```

### 價值鏈:Story 到 Test Case

```
Story              這次要交付什麼使用者價值
  ↓
FR / NFR / Rules   系統要做到什麼、做到什麼程度
  ↓
Acceptance Criteria  滿足哪些條件才算完成
  ↓
BDD Scenario       用 Given / When / Then 驗證這些條件
  ↓
Test Case          用具體資料與操作實際執行
  ↓
結果回流            執行 → 缺陷 → 修復 → 重驗
```

**最後那道回流才是價值閉環與數據飛輪的基底** —— 也是本系統目前最完整的部分。

### 系統的對應能力

| 層級                    | 系統對應                                             | 狀態                              |
| ----------------------- | ---------------------------------------------------- | --------------------------------- |
| Business Goal           | Initiative(workspace 層)                             | ✅                                |
| Epic                    | Work item type(`IssueType.is_epic` / `level`)        | ✅                                |
| Feature                 | Work item type,`Issue.parent` 指向 Epic              | ✅ 資料層可任意深度               |
| Story                   | Work item type,`parent` 指向 Feature                 | ✅                                |
| FR / NFR 分類           | Work item property(select)                           | ✅                                |
| Acceptance Criteria     | **Test case 本身**                                   | ✅ 系統設計即如此                 |
| BDD Given / When / Then | `preconditions` + `steps[{action, expected_result}]` | ✅ 對應乾淨                       |
| Test Case 具體資料      | 自由文字 JSON                                        | ⚠️ 無 Scenario Outline / Examples |
| 結果回流                | run → result(append-only)→ defect → 重驗             | ✅ **最完整**                     |

**刻意的結構壓縮**:本模型把 FR / AC / BDD / Test Case 四層壓縮成 **test case 一個物件**。這與 role-playbooks 的立場一致(「Acceptance contract = Test case linked to the requirement」)。若追蹤重心放在 Feature → Story 的價值交付,這個壓縮是划算的;若需要 `FR-XXX-001` 這種可獨立追蹤的識別碼,FR 就得各自成為 work item,數量會顯著膨脹。**這是取捨,不是缺陷。**

### NFR 驗收的四種形態

NFR **不是「跑一次測試就驗完」**。實務上有四種節奏與證據來源都不同的形態:

| 形態           | 例子                                | 驗證方式                                    | 節奏                            | 系統支援                       |
| -------------- | ----------------------------------- | ------------------------------------------- | ------------------------------- | ------------------------------ |
| **1 門檻量測** | P95 < 2s、50 TPS、千萬筆資料下可查  | k6 / JMeter / Locust 產生**量測值**比對門檻 | release candidate,非每次 commit | ✅ 今天即可                    |
| **2 掃描**     | 密碼不得明文、TLS 1.2+、無已知 CVE  | SAST / DAST / 依賴稽核                      | 每次 PR / nightly               | ⚠️ 可轉 JUnit 上傳,通常留在 CI |
| **3 審查**     | 可維護性、可觀測性、稽核保留 365 天 | checklist / ADR / 架構審查                  | 一次性或架構變更時              | ❌ 無 checklist 物件           |
| **4 持續 SLO** | 可用性 99.9%、RPO 15min / RTO 2h    | 生產環境持續量測 + DR 演練                  | 持續 + 定期演練                 | ❌ 無                          |

**第 4 類在出貨前根本無法「測試」** —— 可用性是上個月的量測結果,RTO 要靠演練證明。這類 NFR 永遠不該變成 test case;硬做會得到一個每天「執行」卻不代表任何一次測試的假 case。

形態 1 今天就能完整落地:k6 輸出 JUnit XML → `automation_upload_junit` 帶穩定 `external_id` → 每次執行掛回同一個 NFR case,其歷次結果即效能趨勢。

```
Test case: NFR-PERF-001 生產履歷查詢 P95 < 2s
  Given   資料庫有 1,000 萬筆履歷
  When    以有效工單編號執行查詢(200 VU,持續 5 分鐘)
  Then    P95 回應時間 < 2000ms
```

**因此 NFR 的真正缺口不是「不能寫 NFR test case」,而是 release gate 沒有外部證據接口** —— 見 #15。

### 術語:為何只動呈現層

SDK / CLI / MCP 的工具名稱與輸入結構是**公開契約**(見 codebase-map:removals/renames 需 major version + migration note)。把 `test_run_create` 改成 `test_execution_create` 會打斷所有 agent、CI 腳本與 CLI 使用。

**術語改造一律只動呈現層。** 而 testing UI 目前完全沒接 i18n(其他功能都有 `packages/i18n/src/locales/*/`,testing 僅在 `navigation.json` 有一個 key),因此改術語的正確做法是**先接 i18n**,見 #16。

`Cycle`(Sprint)與 `Module`(Epic / Feature)屬 Plane 上游核心概念,全面改名會波及整個 app 並製造大量上游 rebase 衝突 —— 建議僅以 i18n 覆寫顯示名稱,改資料檔而非程式碼。

### FR 與 NFR 的介面配置:三分,不是二分

FR 與 NFR 的差異夠大,但**不足以構成兩個並列頁面**。最大的差異在「結果的形狀」:

|          | FR                | NFR                                         |
| -------- | ----------------- | ------------------------------------------- |
| 歸屬層級 | 綁單一 Story      | 可能綁 Story / Feature / System **三層**    |
| 結果形狀 | pass / fail(布林) | **量測值**(P95 = 1,840 ms)—— 有數值、有趨勢 |
| 驗收節奏 | 隨 Story 完成     | 四形態各異(per-PR / per-RC / 持續)          |
| 執行環境 | 一般測試環境      | prod-like 資料量 + 負載工具                 |
| 生命週期 | Story 關閉即結束  | **長期存在,持續回歸**                       |

**業界已收斂**:TestRail、qTest、PractiTest 皆採「單一 case repository + `type` 欄位 + 儲存檢視」;Jira + Xray 則把效能測試放在體系外只回傳摘要。**沒有主流工具把 FR 與 NFR 做成兩個對等頁面**,因為那會把一個需求的驗收證據拆到兩處。

分成兩頁在本系統會直接違反自訂判準:違反「每一個關聯都要雙向可走」(同時有 FR 與 NFR 的 Story 要看兩處)、coverage 需跨兩邊合併(#14 已在處理階層維度,再加一維更難)、release gate 需讀兩個來源。

但直接混進同一張表同樣不可行:FR 不需要「門檻」欄位,NFR 不需要資料夾分類卻很需要「最新量測值」。

**因此依四形態切成三份:**

| 內容                             | 放哪                                               | 理由                             |
| -------------------------------- | -------------------------------------------------- | -------------------------------- |
| 功能驗收(FR)                     | Test case,`type = functional`                      | 現況                             |
| **可量測的 NFR**(形態 1、2)      | **同一個 case 庫**,`type = performance / security` | 會被執行、會產生結果、需要回歸   |
| **持續 / 審查型 NFR**(形態 3、4) | **不進 case 庫** → Release Gate 外部證據           | 出貨前無法測試,硬做會產生假 case |

#### 介面配置

**Case detail 依 `type` 換渲染。** 功能型維持 Given / When / Then 加 pass-fail 時間軸;門檻型改為:

```
Given     1,000 萬筆履歷 · 200 VU · 持續 5 分鐘
When      以工單編號查詢
Then      P95  <  2,000 ms          ← 結構化欄位,非自由文字
─────────────────────────────────
最新量測   1,840 ms   達標   餘裕 8%
趨勢      最近 10 次折線
來源      k6 · CI run #482
```

門檻必須結構化為 `{metric, operator, threshold, unit}`,量測值為 `{measured, unit}`。塞在自由文字裡就畫不出趨勢、也無法自動判定。**`expected_result` 與 `actual_result` 皆已是 JSONField,不需 migration** —— 與 rich text 的情況相同,缺的只是渲染與寫入端。

**Case 清單不分頁,加 `type` 欄位與檢視切換**(全部 / 功能 / 效能 / 安全),清單欄位隨檢視變換:功能看 priority,門檻看最新量測。

**Overview 新增「出貨門檻」區塊**,把 #15 從抽象接口變成可見的東西:

```
出貨門檻
  功能測試       42/42 通過
  效能門檻        3/3 達標
  未結缺陷        1 件 · DEMO-5
  安全掃描        0 高風險            ← 外部證據
  SLO 上月達標     99.7% < 99.9%      ← 外部證據
  架構審查簽核     待簽                ← 外部證據
```

前三行來自 test management,後三行來自外部來源。

#### 一個容易混淆的區分

**work item 上的 FR / NFR 分類 ≠ test case 上的 `type`。**

- work item 的分類 → 這條**需求本身**是功能還是品質要求(需求管理)
- test case 的 `type` → 這個**驗收契約用什麼方式驗**(測試管理)

兩者不同:一條功能需求的驗收條件完全可能包含一個效能門檻。**不要用同一個欄位表達兩件事。**

---

## 附錄 D · DEMO 範例專案

`DEMO / Traceability Demo` 是依第 5 節結構建立的示範專案,同時**刻意重現 backlog 上的缺口**,讓它們可被觀察而非只能被論述。

結構:

```
Epic     生產履歷與異常追溯能力
├── Feature  工單與序號生產履歷查詢
│   ├── Story  IE 以工單編號查詢生產履歷        3 個契約
│   ├── Story  IE 以產品序號查詢生產履歷        1 個契約
│   └── 品質需求  履歷查詢 P95 < 2s            1 個門檻契約
└── Feature  NG 事件修正與審核
    ├── Story  IE 修正 AI 誤判結果             2 個契約(1 失敗 → 缺陷 → 重驗)
    └── Story  主管審核修正紀錄                 0 個契約 ← 刻意留白
```

一輪驗證釘住 8 個契約版本,產生 9 筆結果(含 1 筆重驗追加)。

### 它證實了什麼

執行 `requirement-coverage` 與 `overview` 後的實際輸出:

| 觀察                                                                                                                           | 缺口                             |
| ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------- |
| Epic 與兩個 Feature 皆顯示 **UNCOVERED**,儘管其下共有 8 個契約                                                                 | #14 無階層 roll-up               |
| service 建立的缺陷(未標型別)也被列為待覆蓋的需求                                                                               | #14 無型別過濾                   |
| `coverage_percent` 回報 **100%**,同時有 4 個 work item 未覆蓋 —— 它算的是「已連結需求的 case 佔比」,不是「已被覆蓋的需求佔比」 | #14 指標方向相反                 |
| `release_gate.ready = true`,儘管有一個 Story 零驗收契約                                                                        | #18 gate 不看覆蓋率,DoR 未被強制 |
| 門檻契約帶著結構化的 `{metric, operator, threshold, unit}` 與量測值 `{measured: 1840, unit: "ms"}`,UI 只當純文字顯示           | #17 缺 type 與門檻渲染           |

**#14 與 #18 的嚴重度是依這份實測結果訂的,不是推估。**

### 重建方式

```bash
python manage.py seed_testing_demo --workspace <slug> [--identifier DEMO] [--owner <email>] [--force]
```

同識別碼的專案已存在時會拒絕執行,必須明確加上 `--force` 才會取代 —— 它會連同該專案的所有 work item、測試契約、執行與結果一併刪除。整個 seeding 在單一 transaction 內完成。

所有測試資產皆透過 `plane.testing.services` 建立,因此版本不可變、run 釘版本、結果僅追加、缺陷僅能從 failed/blocked 產生這些不變量都會被實際強制,不會產生繞過服務層的髒資料。

---

## 下一步

建議的實作順序:

1. ~~**#11 路由與深連結**~~ —— ✅ **已完成**,三個介面的前置條件已就緒
2. **#16 UI 接 i18n 並改用軟體領域術語** —— 只動呈現層、風險最低,且會**逼我們先把 Epic / Feature / Story 的詞彙定下來**,那正是 #14 的前置條件
3. **#14 coverage 報表修正** —— 階層 roll-up、型別過濾、指標方向;沒有它就無法確認 Feature 層級的價值交付
4. **#18 gate 納入覆蓋率** —— 緊接 #14,讓 DoR 真的擋得住出貨
5. **#17 case `type` 與結構化門檻** —— NFR 進得了系統的前提;`expected_result` / `actual_result` 已是 JSONField,不需 migration
6. **#1 #4 需求連結** —— 改用搜尋選取與可點 chip,純複用既有元件,見效最快
7. **#2 → #3 證據鏈** —— 先做附件能力,再讓它完整跨越交接點
8. **#5 #6 兩端的回頭路** —— 已被 #11 解鎖,可與第 6 項並行
9. **#15 release gate 外部證據接口** —— 需 #17 先落地才有量測可引用
10. **#9 #10 #12 排一輪驗證的體驗**

每一項各自走一輪 spec → plan → 實作。

視覺版(含 JTBD 地圖、斷點標示、介面線框、NFR 四形態與 DEMO 結構):<https://claude.ai/code/artifact/2ae97264-65a4-4a77-b111-24bc7cbc6ea4>

**本檔為唯一真相源。** 視覺版依需要重新產生,不做逐次同步 —— 逐次手工同步兩份內容已證實會累積落差。
