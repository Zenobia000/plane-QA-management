# Upstream merge rehearsal — 2026-07-14

Scope: WBS 7.5, rehearsing an upstream update without modifying the dirty localhost worktree or its index.

## Baseline and isolation

- Fork baseline: `d3d3de44cf13991025783c598d8b34229fb47729` (`preview`).
- Upstream target: `origin/master` at `d0a4adc55bb1fa5b8c221c0c680b992d2677e9c5`.
- Merge base: `1dabc632`; divergence was 77 fork commits and 2 upstream commits.
- Captured the complete current worktree in a temporary index and synthetic commit
  `9243a6156c9ba26fd36a63fbcf41c689295961c1`, then created a detached worktree at
  `/tmp/plane-upstream-rehearsal`.
- The real worktree and index were not reset, committed, or merged.

## Merge result

- `git merge --no-commit origin/master` completed automatically with no conflicts.
- Rehearsal merge commit: `09d4771a0ad0f603da09839c3a897c6283e81167`.
- `git diff --check HEAD^1..HEAD` passed.
- `git diff --quiet HEAD^1 HEAD` passed: the two upstream commits introduced no file-tree delta relative to the
  captured fork snapshot, so the previously completed current-copy migration drill applies to the merged tree.
- The rehearsal worktree was clean after the merge commit.

## Affected-suite verification

1. `pnpm install --frozen-lockfile` passed.
2. `pnpm turbo run build --filter=web` passed: 11 of 11 tasks completed, including the production Web build.
3. `pnpm --filter ./apps/web test` passed: 2 files and 6 tests.
4. `pnpm --filter ./apps/web check:types` passed.
5. A distinct `plane-upstream-tests` Compose project built the API test image from the merged worktree and ran the
   Testing unit and contract suites against disposable PostgreSQL, Valkey, RabbitMQ, and MinIO services: 28 tests
   passed in 12.85 seconds. The 13 warnings were Django-model pytest collection warnings, not failed tests.
6. The disposable containers, volumes, and network were removed after the run.

The first direct Web test/type attempt occurred before internal workspace packages were built and could not resolve
`@plane/*` packages. Running the documented prerequisite production build populated those packages; both checks then
passed. This was an isolated-worktree setup prerequisite, not a merge regression.

## Repeatable update procedure

1. Fetch upstream history and record the fork commit, upstream commit, merge base, and divergence.
2. Snapshot all intended fork changes in an isolated branch/worktree; never rehearse against a user's dirty index.
3. Merge the selected upstream ref without committing, record conflicts and resolutions, then create the rehearsal
   merge commit.
4. Run `git diff --check`, install with the frozen lockfile, build prerequisite workspace packages and the production
   Web bundle, then run Web tests/type checking and the affected API suites in isolated services.
5. If migrations differ, repeat the current-copy upgrade rehearsal before accepting the merge.
6. Record evidence and remove disposable services/worktrees only after results are captured.

## Result

PASS. The selected upstream update merges cleanly and the full affected Testing suite passes on the merged snapshot.
