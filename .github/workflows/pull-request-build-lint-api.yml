name: Build and lint API

on:
  workflow_dispatch:
  pull_request:
    branches:
      - "main"
      - "preview"
    types:
      - "opened"
      - "synchronize"
      - "ready_for_review"
      - "reopened"
    paths:
      - "apps/api/**"
      - "docker-compose-test.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-api:
    name: Lint API
    runs-on: ubuntu-latest
    timeout-minutes: 25
    if: github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12.x"
          cache: 'pip'
          cache-dependency-path: 'apps/api/requirements.txt'
      # Pinned to the version apps/api/requirements/local.txt installs and the
      # pre-commit hook runs. An unpinned install here meant CI could lint with a
      # ruff nobody has locally, so a rule added in a new release would land as a
      # red build on a change that passed every check its author could run.
      - name: Install ruff
        run: python -m pip install ruff==0.9.7
      - name: Install API Dependencies
        run: cd apps/api && pip install -r requirements.txt
      # No --fix. A fix applied here is thrown away with the runner, so
      # `ruff check --fix` reported success for anything auto-fixable while the
      # violation stayed in the commit -- the build passed on a working copy that
      # never existed anywhere else. The pre-commit hook keeps --fix, where the
      # result is staged and kept.
      - name: Lint apps/api
        run: ruff check apps/api

  # The suite needs Postgres, Valkey, RabbitMQ and MinIO. docker-compose-test.yml
  # already wires them with health checks, so CI runs the same command a developer
  # runs locally rather than a second, drifting definition of the environment.
  test-api:
    name: Test API
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v6

      # apps/api/.env is gitignored, and the compose file reads it for the
      # service credentials. The example holds no SECRET_KEY, and settings fall
      # back to a generated one, which is what a test run wants.
      - name: Create the API env file
        run: cp apps/api/.env.example apps/api/.env

      - name: Run pytest
        run: docker compose -f docker-compose-test.yml run --rm api-tests pytest

      - name: Tear down
        if: always()
        run: docker compose -f docker-compose-test.yml down -v
