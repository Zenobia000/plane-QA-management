# Project Overview:從功能對齊改成產品戰情室

> 狀態:**四個階段皆已實作** · 2026-08-03,另有兩項後續變更 · 2026-08-07
> 前身:ADR 0005(Overview 的後端)、`testing-product-definition.md`(價值鏈與 persona)
> 實作:`46a96c1508`(階段一)、`81e8d808ab`(階段二至四)、`670dd4bee`(比例對帳)、
> `a31fbd60c` + `d8baf761c`(公佈欄形狀)——後兩項見〈[08-03 之後的變更](#08-03-之後的變更)〉

## 為什麼要改

現在的 Overview 來自 enterprise-parity 計畫,所以它報告的是**商業版 Plane 報告的東西** ——
六塊並排的資料,沒有回答任何一個具體問題。

它要服務的其實是兩種人,而兩種人問的是同一串問題:

| 誰       | 問什麼                                      |
| -------- | ------------------------------------------- |
| 管理階層 | 產品開發到哪了?什麼時候能出貨?              |
| 產品經理 | 推進有沒有在規劃上?客戶在抱怨什麼,接住了沒? |

還有一條**跨職能**的要求:前線業務導入時,客戶反映的抱怨要能及時傳到工程團隊。
所以這頁不能只有工程項目 —— 市場、商務、客戶緊急事件都要有位置。

## 設計原則:三類資料,三種互動規則

這是整份規格的骨幹。每一個區塊都必須先歸類,才能決定它長什麼樣。

| 類別       | 定義                    | 互動規則                       | 為什麼                             |
| ---------- | ----------------------- | ------------------------------ | ---------------------------------- |
| **A 衍生** | 從別的資料算出來        | **不可在此編輯**,只能點進源頭  | 讓人改算出來的數字等於製造兩個真相 |
| **B 分流** | 記錄已存在,人改它的狀態 | **一個動作**,不是表單          | 是狀態轉移,不是重新輸入            |
| **C 原創** | 人在這裡產生第一手內容  | **完整 CRUD**:發布、編輯、刪除 | 這裡就是它的家                     |

違反這條規則的設計會出現的症狀:算出來的數字旁邊放一個可編輯欄位,兩者永遠可以互相矛盾
而沒有人負責。現在的 Properties 面板就是這樣被拿掉的。

## 版面

由上而下,依照「管理階層依序會問的問題」排列。

```
┌────────────────────────────────────────────────────────────┐
│  Order Service Platform                                     │
│  ⚠ 尚不可發布 · 6 項阻擋                    [開啟 Testing ›] │  A
└────────────────────────────────────────────────────────────┘

┌──────────────── 客戶前線 ──────────────┐ ┌─── 進度 ────┐
│ 待處理 5 · 已排入 12 · 已婉拒 3          │ │ 32% (6/19)  │  A
│                                          │ └─────────────┘
│ ▸ 客戶A  3 件  ⚠ 2 待處理  [正式上線]   │  B
│    · 報表匯出逾時       [已排入 08A]    │ ┌── 里程碑 ───┐
│    · 登入偶發失敗       [待處理 5 天]   │ │ M1 3/4      │  A
│ ▸ 客戶B  2 件  ✓ 全數已排入  [POC]      │ │ M2 1/5      │
│                        [開啟 Intake ›]  │ └─────────────┘
└──────────────────────────────────────────┘

┌──────────────── 需要注意 ──────────────┐ ┌── 會議紀錄 ──┐
│ 逾期 3 · 緊急 2                          │ │ 週會 08/01   │  C
│ · [逾期 5 天] 審核回寫金流      @sunnie │ │  [Pages ›]   │
│ · [urgent]   區域授權驗證        未指派 │ └──────────────┘
└──────────────────────────────────────────┘  A

┌──────────────── 公佈欄 ────────────────────────────────────┐
│ [全部] [重要] [市場] [商務] [客戶事件]        ＋ 發布公告    │  C
│ 排序:最新 ▾    分組:狀態 ▾              (每段上限 3 則)     │
│ 🔴 客戶事件 · sunnie · 08/03                        ⋯ 編輯  │
│ 客戶A 回報匯出無法使用,已建 Intake #142                     │
│   ↳ jim:已排入 Sprint 08A                          [回覆]  │
└──────────────────────────────────────────────────────────────┘

┌─ 最近動態(Activity)── 收合,空的時候只有一行 ─────────────┐  A
```

## 逐區塊:讀哪來、誰寫、寫去哪

| 區塊          | 類  | 讀                                   | 寫                                | 狀態 |
| ------------- | --- | ------------------------------------ | --------------------------------- | ---- |
| 出貨判定      | A   | `testing/overview` 的 `release_gate` | 無                                | ✅   |
| 進度 / 里程碑 | A   | `overview`                           | 無                                | ✅   |
| 需要注意      | A   | `attention/`                         | 無(點進工作項改)                  | ✅   |
| 客戶前線      | B   | `frontline/`                         | `PATCH intake-issues/<issue_id>/` | ✅   |
| 客戶標記      | B   | `WorkItemPropertyValue`              | `work-item-property-values`       | ✅   |
| 公佈欄        | C   | `EntityUpdate`                       | POST / PATCH / DELETE             | ✅   |
| 會議紀錄      | C   | Pages                                | Pages 全套                        | ✅   |

**API 的寫入路徑幾乎都在**,缺的是 UI 與兩個欄位(見下)。

一個實作時才確認的細節:`PATCH intake-issues/<id>/` 的 `<id>` 是**工作項的 id**,
不是 intake 列的 id —— 端點自己是用 `issue_id` 反查的。傳錯會拿到 404,
讀起來像權限問題。所以 `frontline/` 兩個 id 都回。

## 需要的 schema 變更

規劃時預期只有一項。實作到階段二才發現第二項:**面板要能找到「客戶」這個屬性**。
屬性由使用者自己建立,名字可能叫客戶、租戶、地區或試點批次 —— 程式不能猜。
所以需要一個地方記下「使用者選了哪一個」,否則「做成活的」只做了一半:
欄位是活的,但沒有人告訴畫面要讀哪一欄。

解法是 `WorkItemProperty.is_grouping_dimension`,每專案至多一個,只允許 select 類型
(用自由文字分組會每個錯字生一組)。**選擇本身是資料,存在專案層**,不是每個閱讀者
各自的偏好 —— 大家看的應該是同一塊板子。

### 公佈欄分類 → 掛 Label,不要新 enum

`EntityUpdate.status` 只有 `on_track / at_risk / off_track`,那是**專案健康度**不是主題分類,
不能挪用。

分類必須是**活的** —— 使用者自己增刪,像 Notion。`Label` 已經滿足:每專案獨立、有顏色、
有階層(`parent`)、設定頁現成。

所以只加一張 join table:

```
EntityUpdateLabel(entity_update, label, project, workspace)
```

**結構動一次 migration,分類值永遠由使用者決定。** 沒有任何分類名稱寫進程式碼。

### 客戶 → 用既有的 WorkItemProperty,加一個指標欄位

`WorkItemProperty` 已經支援 7 種型別、可綁定特定 work item type、可設必填與預設值。
屬性本身完全由使用者在設定頁建立,程式不預設任何一個。建議(不是預設值):

| 屬性       | 型別                             | 它改變什麼決定                        |
| ---------- | -------------------------------- | ------------------------------------- |
| 客戶       | `multi_select`                   | 哪個客戶在抱怨 → 分組、看單一客戶全貌 |
| 導入階段   | `select`:POC / 試營運 / 正式上線 | 同一個 bug,正式上線客戶回報的先修     |
| 客戶承諾日 | `date`                           | **已存在**;有無對外承諾 → 排程硬約束  |

綁在 Bug 與 Intake 相關的 type 上,不要綁全部 —— 內部技術債不該被問哪個客戶。

唯一寫進 schema 的是**指標**:哪一個屬性拿來分組(`is_grouping_dimension`)。
屬性的名字、選項、顏色一律是資料。

專案層而非 workspace 層:專案彼此獨立,同一個客戶若跨專案合作就各自建立。等到跨專案
查詢真的變成需求,再升級成 workspace 實體,那時才會知道要存什麼欄位。

## 分階段

每一階段都可獨立出貨、獨立 revert。四個階段皆已完成。

**階段一 · 公佈欄成形**(核心訴求)✅

1. ✅ 公佈欄移到 ProgressBar 之上,支援編輯與刪除(`deleteUpdate` 原本是死程式碼)
2. ✅ 編輯過的公告顯示「已編輯」——`EntityUpdate.edited_at`,只在改文字時蓋章;
   掛標籤是歸檔不是改稿,不蓋章
3. ✅ 權限:作者本人與 ADMIN,在 `partial_update` / `destroy` 強制
4. ✅ `EntityUpdateLabel` + 分類篩選列

**階段二 · 客戶前線** ✅

1. ✅ 改為 `is_grouping_dimension`:使用者建屬性、在設定頁指定哪一個是分組維度
2. ✅ `GET frontline/`:依該維度分組,每組 待處理 / 已排入 / 已婉拒 + 前 5 筆
3. ✅ 面板 + 一鍵分流(`PATCH intake-issues/<issue_id>/`,ADMIN 才顯示按鈕)

**階段三 · 需要注意** ✅

1. ✅ `GET attention/`:逾期優先於緊急,逾期內最舊者優先,最多 5 筆;
   已完成/已取消排除;`total_overdue` / `total_urgent` 說明被截掉多少
2. ✅ 面板,每筆可點進工作項

**階段四 · 收尾** ✅

1. ✅ 會議紀錄連到 Pages(連結而非嵌入清單 —— 嵌入等於在 Overview 放第二個更差的
   Pages 索引)
2. ✅ Activity 收合到底部,預設收起

## 實作時偏離規格之處

- **`show all` 之後的分類篩選在前端做**。板子只有十則,為了隱藏兩則多跑一次
  round trip 比點擊本身還慢。端點仍支援 `?label=`,給未來的分頁用。
- **客戶屬性沒有預先建立**。規格寫「建三個屬性(設定頁操作)」,實作改成
  什麼都不預設 —— 預先建立等於替使用者決定他的客戶叫什麼,正是這份規格要避免的。
- **前線面板沒有渲染其他屬性(導入階段、承諾日)的 chip**。列表已有五個欄位,
  再加兩個 chip 會讓一行讀不完。點進工作項就看得到。

## 08-03 之後的變更

四個階段出貨後,這一頁被真的拿來讀,兩件事才顯出來。

### 一、三個比例各自說出它在數什麼(`670dd4bee`,2026-08-07)

這一頁是拿來下出貨決定的,所以「兩個數字讀者對不起來」就是它自己要防的失敗。三處:

| 比例          | 原本的問題                                                                         | 現在                                                       |
| ------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 最近一次執行  | 分母是 `passed + failed + blocked`,漏掉 `open` 與 `skipped` —— 二十個 case 有九個沒跑會顯示「8/11」,讀起來像快做完了,而正上方的出貨判定同時列出那九個當阻擋 | 分母是整個 run;未執行與略過的數量用出貨判定自己的措辭列在比例旁邊,不藏在 tooltip |
| 客戶前線摘要  | 摘要一則報告算一次,底下的分組標題依歸屬的帳號各算一次 —— 四組各兩則卻描述七則報告 | 端點加 `totals.reports`(去重後的則數)與 `totals.multi_attributed`(被算超過一次的則數),面板把算術直接寫出來;**既有 key 未變** |
| 需求覆蓋 / 進度 | 兩者都是「X / Y」但母體不同(已排期且需要驗收契約的需求 vs 全部未取消的工作項),兩個都對,而兩個都沒說 | 各自帶自己的定義                                           |

**分組會重複計數不是錯,是歸屬的必然** —— 一個 bug 兩個客戶都報過,它本來就屬於兩組。
所以解法是讓面板把差額說出來,不是把其中一個數字改成另一個。

由此多一條版面規則,與「A 類不可編輯」並列:**同一頁上任何兩個比例,只要母體不同,
就各自寫出母體。** 讀者不該被迫自己對帳。

### 二、公佈欄從流水帳變成有形狀的板子(`a31fbd60c`、`d8baf761c`,2026-08-07)

原本只有「最新在上」一種讀法。三則沒問題,三十則就沒用:那則會改動出貨日期的客戶事件,
沉在四則沒人在等的公告底下,而唯一找到它的方法是全部讀一遍。

三個控制項,全部是讀者端,**無 API 變更、無 schema 變更**:

- **排序**:最新 / 最舊 / 最嚴重優先(`off track` > `at risk` > `on track`,同狀態以較近者優先)
- **分組**:依狀態或依主題,每段可收折並帶自己的計數 —— 「off track 有四則」本身就是大半個看板的價值
- **每段上限三則**,其餘一鍵展開。與既有的「載入更早」不同:那個會抓資料,這個只是攤開已有的

兩個刻意的行為:

- **改排序會先強制完整載入。** 把四十則裡的十則排完序後叫做「最嚴重優先」,等於把讀者要找的
  那則留在沒抓下來的三十則裡。預設檢視(最新在上、不分組)例外 —— 那就是端點回傳的順序,
  所以先載入的那一頁是完整排序的真前綴。
- **依主題分組時一則公告出現在它帶的每個主題底下**,這是「同時關於客戶承諾與跨團隊相依」
  這種公告的誠實畫法。因此各段計數加起來會大於板子的則數,面板明講這件事 —— 與上面客戶前線
  同一套處置。

排序與分組規則放在 `core/components/updates/shape.ts` 的純函式裡而不是 JSX 中:
「off track 排在 at risk 前面」是產品主張,應該被直接斷言。

**已知未做**(不影響上述行為,但下一個動這塊的人該知道):`panel.tsx` 700 行,低於本 repo
800 行上限、高於偏好的 200–400,composer、主題篩選列與回覆串是接下來該拆出去的三塊;
控制項目前只有 static render 測試,**點下去做了什麼沒有被測** —— 此 repo 的 vitest 環境是
`node`、沒有 DOM。

## 不做什麼

- **不做跨專案客戶視圖**。專案獨立是前提;跨專案是另一個頁面,等這個模式證明有用再說。
- **不加 CRM 欄位**(聯絡人、合約金額、產業別)。它們不會改變工程接下來做什麼。
  沒人填的欄位比沒有欄位更糟,因為它看起來像資料。
- **不讓 A 類區塊可編輯**。要改就點進源頭改。

## 驗收

- 公佈欄:發布、編輯、刪除、回覆、依分類篩選;非作者非 ADMIN 看不到編輯入口
- 客戶前線:標了客戶的 Intake 會分組;一鍵把待處理改成已排入,列表即時反映
- 需要注意:逾期與緊急各自出現,點擊進入該工作項
- 沒有測試資料的專案不顯示出貨判定;沒有 Intake 的專案不顯示客戶前線

自動化的部分在 `apps/api/plane/tests/contract/app/` 兩個檔案共 24 個測試:
`test_noticeboard.py`(發布 / 改稿 / 歸檔 / 撤下 / 跨專案標籤拒絕 / 權限邊界)、
`test_frontline_and_attention.py`(分組 / 多值同時歸屬多組 / 未歸屬不隱藏 /
五種狀態收斂成三種 / 上限與其回報 / 選項被改名仍渲染 / 逾期優先 / 已完成排除 /
同一筆不重複 / 最舊優先 / 指派人)。

## 怎麼開始用

1. 專案設定 → 自訂屬性,建一個 select 或 multi-select,名字自己取(客戶、租戶、地區)
2. 同一列點「Group overview by this」
3. 在 Intake 的工作項上填那個屬性 —— 這一步當初在 UI 上做不到(建立視窗與詳情頁都沒有
   渲染自訂屬性),補在 [`intake-attribution.md`](intake-attribution.md)
4. Overview 出現該面板,標題就是你取的名字

公佈欄的分類同理:專案設定 → 標籤,建幾個自己的分類,發公告時勾選。
兩者都沒有預設值 —— 預設值等於替你決定你的客戶叫什麼。
